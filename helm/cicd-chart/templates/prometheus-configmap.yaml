apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-prometheus-config
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s
      evaluation_interval: 15s
    scrape_configs:
      - job_name: "cicd-server"
        metrics_path: "/metrics"
        static_configs:
          - targets: ["{{ .Release.Name }}-cicd-server-service:{{ .Values.server.service.port }}"]
