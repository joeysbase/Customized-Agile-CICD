apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-loki-service
spec:
  selector:
    app: {{ .Release.Name }}-loki
  ports:
    - port: {{ .Values.loki.service.port }}
      targetPort: 3100
