apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-mongo-service
spec:
  selector:
    app: {{ .Release.Name }}-mongo
  ports:
    - port: {{ .Values.mongo.service.port }}
      targetPort: 27017